# دليل تكامل حالات الدفع الجديدة للفرونت إند / Frontend Payment Status Integration Guide

تم تحديث نظام حالات الدفع (Payment Statuses) الخاص بالطلبات (Orders) والعمليات المالية في الخلفية (Backend) لتوفير تتبع أدق لحالة الدفع، خاصة مع نظام الأقساط (Milestones).

---

## 1. حالات الدفع الجديدة / New Payment Statuses

تم إلغاء الحالة السابقة `Pending` واستبدالها بنظام ثلاثي يوضح حالة الدفع الفعلية للطلب:

| الحالة في الـ API (String) | القيمة المقابلة (Enum) | الترجمة العربية / Arabic | TR / English | الوصف / Description |
| :--- | :---: | :---: | :---: | :--- |
| **`Unpaid`** | `0` | لم يتم الدفع | Unpaid | الحالة الافتراضية للطلب عند إنشائه وقبل دفع أي مبلغ. |
| **`PartialPaid`** | `1` | جاري الدفع | Partially Paid | يتم تفعيلها عند دفع أي جزء (قسط أو أكثر) ولم يكتمل إجمالي قيمة الطلبات الفرعية المعتمدة بالكامل. |
| **`Paid`** | `2` | تم الدفع | Paid | يتم تفعيلها عند سداد كامل قيمة الطلبات الفرعية المعتمدة بالكامل (جميع الأقساط للطلبات التي تمت الموافقة عليها). |
| **`Failed`** | `3` | فشل الدفع | Failed | في حال فشل عملية الدفع عبر بوابة الدفع. |
| **`Cancelled`** | `4` | ملغي | Cancelled | في حال إلغاء المعاملة المالية. |

---

## 2. التغييرات في الـ API Responses

ستحتوي استجابات الـ API الخاصة بالطلبات (مثل تفاصيل الطلب أو قائمة الطلبات) على حقل `paymentStatus` بالقيم النصية التالية:

```json
{
  "id": 105,
  "totalPrice": 1500.00,
  "status": "Processing",
  "paymentStatus": "PartialPaid", // القيم الممكنة: "Unpaid" | "PartialPaid" | "Paid" | "Failed" | "Cancelled"
  "createdAt": "2026-06-28T13:30:00Z"
}
```

---

## 3. منطق الانتقال بين الحالات / State Transition Logic

1. **عند إنشاء الطلب (Order Creation):**
   - تكون حالة الدفع تلقائياً `Unpaid`.
2. **الدفع الجزئي (Partial Payment):**
   - إذا قام العميل بدفع أي جزء (قسط أو أكثر) ولم يكمل كامل إجمالي قيمة الطلبات الفرعية المعتمدة (Approved Vendor Orders)، تصبح الحالة `PartialPaid`.
3. **اكتمال الدفع للطلب بالكامل (Full Payment):**
   - تصبح الحالة **`Paid`** فقط عندما يتساوى إجمالي المبالغ المدفوعة في الـ Milestones مع إجمالي قيمة **جميع الطلبات الفرعية المعتمدة** (وهي الطلبات التي تمت الموافقة عليها من قبل العميل وليست Cancelled أو Pending أو AwaitingCustomerApproval).
4. **النظام التقليدي (Legacy Flow - بدون أقساط):**
   - ينتقل الطلب مباشرة من `Unpaid` إلى `Paid` عند نجاح الدفع الكامل للطلب.

---

## 4. نصوص الترجمة المقترحة للفرونت إند / Localization Keys

إذا كان الفرونت إند يستخدم ملفات ترجمة مستقلة، يرجى إدراج المفاتيح التالية:

### العربية (`ar.json`)
```json
{
  "payment": {
    "status": {
      "unpaid": "لم يتم الدفع",
      "partialPaid": "جاري الدفع",
      "paid": "تم الدفع",
      "failed": "فشل الدفع",
      "cancelled": "ملغاة"
    }
  }
}
```

### English (`en.json`)
```json
{
  "payment": {
    "status": {
      "unpaid": "Unpaid",
      "partialPaid": "Partially Paid",
      "paid": "Paid",
      "failed": "Failed",
      "cancelled": "Cancelled"
    }
  }
}
```
