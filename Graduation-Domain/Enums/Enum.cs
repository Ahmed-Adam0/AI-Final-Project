using System;
using System.Collections.Generic;
using System.Text;

namespace Graduation_Domain.Enums
{
    public enum OrderStatus
    {
        Pending = 0,
        Confirmed = 1,
        InProgress = 2,
        ReadyForPickup = 3,
        Delivered = 4,
        Cancelled = 5
    }

    public enum Enum
    {
    }
}
