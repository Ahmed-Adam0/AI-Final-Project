# دليل تكامل نظام الـ Showcase / الصفحة الرئيسية (Home Showcase API Integration Guide)

يوثق هذا المستند واجهات برمجة التطبيقات (APIs) الخاصة بنظام الـ **Showcase (البنرات التفاعلية)** في الصفحة الرئيسية، والمخصصة للاستخدام من قِبل فريق التطوير (Frontend).

نظام الـ Showcase مبني على أن يقوم **البائعون (Vendors)** بإدارة بنراتهم التفاعلية الخاصة بورشهم، بينما يتصفح **العملاء (Customers)** البنرات النشطة بشكل عام. يحتوي كل بنر على نقاط تفاعلية (Hotspots) تشير إلى منتجات معينة معروضة في صورة البنر لتمكين المستخدم من الشراء السريع.

---

## 🚀 ملخص هيكلية البيانات (Data Contracts)

### 1. كائن السلايد التفاعلي (Showcase Slide DTO)
عند جلب السلايدز، تعود البيانات بالهيكل التالي:

```json
{
  "id": 1,
  "title": "العنوان المترجم بحسب لغة الطلب",
  "subtitle": "العنوان الفرعي المترجم",
  "buttonText": "نص الزر المترجم",
  "buttonLink": "رابط التوجيه عند الضغط",
  "backgroundImageUrl": "رابط الصورة المرفوعة على Cloudinary",
  "displayOrder": 1,
  "isActive": true,
  "workshopId": 5,
  
  // القيم الخام للغتين (مفيدة للوحة التحكم الخاصة بالبائع للتحرير)
  "titleAr": "غرفة معيشة مودرن",
  "titleEn": "Modern Living Room",
  "subtitleAr": "أثاث راقي يناسب ذوقك",
  "subtitleEn": "Elegant furniture that suits your taste",
  "buttonTextAr": "تسوق الآن",
  "buttonTextEn": "Shop Now",
  
  "hotspots": [
    {
      "id": 10,
      "x": 45.50,
      "y": 60.20,
      "displayOrder": 1,
      "isActive": true,
      "product": {
        "id": 101,
        "nameAr": "اسم المنتج بالعربي",
        "nameEn": "اسم المنتج بالإنجليزي",
        "descriptionAr": "وصف المنتج بالعربي",
        "descriptionEn": "وصف المنتج بالإنجليزي",
        "basePrice": 2500.00,
        "mainImageUrl": "/images/products/table.jpg",
        "averageRating": 4.80,
        "availability": true
      }
    }
  ]
}
```

> [!IMPORTANT]
> **إحداثيات النقاط التفاعلية (Hotspots Coordinates):**
> القيم `x` و `y` هي قيم مئوية عشرية تتراوح بين `0.00` و `100.00`.
> في الـ Frontend، يجب رندرتها كـ `absolute` داخل حاوية السلايد ذات الوضعية النسيبية `relative`:
> ```css
> .hotspot-dot {
>   position: absolute;
>   left: calc(x * 1%); /* تحديد موضع المحور الأفقي */
>   top: calc(y * 1%);  /* تحديد موضع المحور العمودي */
>   transform: translate(-50%, -50%);
> }
> ```

---

## 📡 واجهات برمجة التطبيقات (API Endpoints)

### 1. جلب بنرات الصفحة الرئيسية النشطة (Public)

يسترجع هذا الطلب قائمة بجميع بنرات العرض النشطة والمفعلة لجميع الورش لعرضها للزوار.

* **المسار (Route):** `GET /api/Home/showcase`
* **الصلاحية (Authorization):** غير مطلوبة (متاح للجميع)
* **دعم اللغة (Localization):** يعتمد على ترويسة الطلب `Accept-Language` لتحديد لغة الحقول (`Title`, `Subtitle`, `ButtonText`).

#### نموذج الاستجابة (200 OK):
*(يعود بمصفوفة من كائنات الـ Showcase Slide DTO الموضحة أعلاه)*

---

### 2. إنشاء بنر عرض جديد (Secured - Vendor Only)

يسمح هذا الطلب للبائع بإنشاء بنر تفاعلي جديد وإضافة منتجات من ورشته كنقاط تفاعلية على صورة البنر.

* **المسار (Route):** `POST /api/Home/showcase`
* **الصلاحية (Authorization):** مطلوب توكن بائع (`Bearer <JWT_TOKEN>` لدور `Vendor`).
* **نوع المحتوى (Content-Type):** `multipart/form-data` (بسبب رفع الصورة الخلفية).

#### حقول الطلب (Form-Data):

| اسم الحقل (Key) | النوع (Type) | مطلوب؟ | الوصف |
| :--- | :--- | :--- | :--- |
| `TitleAr` | `string` | **نعم** | عنوان البنر باللغة العربية. |
| `TitleEn` | `string` | **نعم** | عنوان البنر باللغة الإنجليزية. |
| `SubtitleAr` | `string` | لا | العنوان الفرعي بالعربية. |
| `SubtitleEn` | `string` | لا | العنوان الفرعي بالإنجليزية. |
| `ButtonTextAr` | `string` | لا | نص الزر بالعربية. |
| `ButtonTextEn` | `string` | لا | نص الزر بالإنجليزية. |
| `ButtonLink` | `string` | **نعم** | الرابط الذي يوجه إليه الزر عند النقر. |
| `BackgroundImage` | `File` | **نعم** | ملف الصورة الخلفية الخاصة بالبنر. |
| `DisplayOrder` | `integer` | لا | ترتيب ظهور البنرات في الصفحة الرئيسية (الافتراضي: `0`). |
| `IsActive` | `boolean` | لا | تفعيل البنر أو تعطيله بشكل مؤقت (الافتراضي: `true`). |
| `HotspotsJson` | `string (JSON)` | لا | سلسلة نصية JSON تمثل مصفوفة النقاط التفاعلية (انظر الهيكل أدناه). |

#### هيكل الـ `HotspotsJson`:
يجب تمرير النقاط التفاعلية المرفقة بالمنتجات كـ JSON String داخل الـ Form-Data كالتالي:
```json
[
  {
    "productId": 101,
    "x": 45.5,
    "y": 60.2,
    "displayOrder": 1,
    "isActive": true
  },
  {
    "productId": 102,
    "x": 12.3,
    "y": 80.0,
    "displayOrder": 2,
    "isActive": true
  }
]
```

> [!WARNING]
> **شروط التحقق من صحة المدخلات (Validation Rules):**
> * يجب أن تكون صورة الخلفية ملف صورة صالح وامتداد مقبول.
> * يجب أن تكون إحداثيات `X` و `Y` لكل نقطة تفاعلية بين `0` و `100` تماماً.
> * يجب أن تكون معرفات المنتجات `productId` موجودة بالفعل في قاعدة البيانات.
> * **مهم جداً:** يجب أن تكون جميع المنتجات المرفقة تابعة لورشة البائع المسجل نفسه. سيقوم النظام بإرجاع خطأ `401 Unauthorized` أو `400 Bad Request` إذا حاول البائع ربط منتج لا يملكه.
> * تتم العملية بالكامل كعملية موحدة (Database Transaction)؛ في حال فشل أي تحقق أو رفع الصورة، يتم إلغاء العملية بالكامل تلافياً لبيانات غير مكتملة.

#### نموذج الاستجابة (201 Created):
يعود بالكائن المضاف حديثاً متضمناً معرّف السلايد الجديد `id` ورابط الصورة المرفوعة على Cloudinary.

---

### 3. تعديل بنر تفاعلي قائم (Secured - Vendor Only)

يسمح للبائع بتعديل بيانات البنر أو استبدال الصورة أو تعديل/إضافة نقاط تفاعلية جديدة.

* **المسار (Route):** `PUT /api/Home/showcase/{id}`
* **الصلاحية (Authorization):** مطلوب توكن البائع مالك السلايد (`Bearer <JWT_TOKEN>`).
* **نوع المحتوى (Content-Type):** `multipart/form-data`

#### حقول الطلب (Form-Data):
نفس حقول طلب الإنشاء تماماً، مع ملاحظة أن حقل `BackgroundImage` يصبح **اختيارياً**؛ إذا لم يتم إرساله، يحتفظ السلايد بالصورة الخلفية الحالية دون تغيير.

> [!NOTE]
> * عند تعديل النقاط التفاعلية، يتم استبدال جميع النقاط القديمة بالنقاط الجديدة الممررة في `HotspotsJson` تلقائياً في معاملة برمجية موحدة.
> * يتحقق النظام من ملكية البائع للسلايد المطلوب تعديله ومنتجاته الجديدة قبل الحفظ.

#### نموذج الاستجابة (200 OK):
يعود بكائن السلايد المعدل بعد الحفظ المباشر.

---

### 4. حذف بنر تفاعلي (Secured - Vendor Only)

حذف السلايد بالكامل مع جميع النقاط التفاعلية التابعة له وصورته من سحابة التخزين.

* **المسار (Route):** `DELETE /api/Home/showcase/{id}`
* **الصلاحية (Authorization):** مطلوب توكن البائع مالك السلايد (`Bearer <JWT_TOKEN>`).

#### نموذج الاستجابة عند النجاح (204 No Content):
*(لا يوجد محتوى في جسم الاستجابة)*

#### الأخطاء المحتملة:
* `401 Unauthorized`: عند محاولة حذف سلايد غير تابع للبائع المسجل.
* `404 Not Found`: إذا كان معرّف السلايد غير موجود.
