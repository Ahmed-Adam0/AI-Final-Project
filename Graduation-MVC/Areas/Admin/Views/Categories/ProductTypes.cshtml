@model IEnumerable<Graduation_Application.DTOs.CategoryDTO.ProductTypeDto>
@using Graduation_Application.IServices.Admin
@inject ILocalizationService L
@{
    var subCategory = (Graduation_Application.DTOs.CategoryDTO.SubCategoryDto)ViewBag.SubCategory;
    ViewData["Title"] = $"Product Types of {subCategory.NameEn}";
}

<div class="mb-3">
    <a asp-area="Admin" asp-controller="Categories" asp-action="SubCategories" asp-route-categoryId="@subCategory.CategoryId" class="btn btn-premium btn-premium-secondary small py-1 px-3">
        <i class="fa-solid fa-arrow-left me-1"></i> Back to Subcategories
    </a>
</div>

<div class="row align-items-center mb-4">
    <div class="col-md-6">
        <h1 class="display-font text-light mb-1">Product Types of @subCategory.NameEn (@subCategory.NameAr)</h1>
        <p class="text-muted mb-0">Manage catalog product types for this subcategory</p>
    </div>
    <div class="col-md-6 text-md-end mt-3 mt-md-0 d-flex justify-content-md-end align-items-center gap-3">
        <div class="glass-card px-3 py-2 d-inline-flex align-items-center gap-3">
            <span class="text-muted small">Total:</span>
            <span class="badge fs-6 font-monospace" style="background-color: var(--accent-primary);">@Model.Count()</span>
        </div>
        <a asp-area="Admin" asp-controller="Categories" asp-action="CreateProductType" asp-route-subCategoryId="@subCategory.Id" class="btn btn-premium btn-premium-primary">
            <i class="fa-solid fa-plus me-1"></i> Add Product Type
        </a>
    </div>
</div>

<!-- ProductTypes Table -->
<div class="glass-card p-4">
    @if (!Model.Any())
    {
        <div class="text-center py-5">
            <div class="text-muted mb-3"><i class="fa-solid fa-folder-open fa-3x"></i></div>
            <h5 class="text-light">No product types defined</h5>
            <p class="text-muted small">Click the button above to add a product type for @subCategory.NameEn</p>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-premium">
                <thead>
                    <tr>
                        <th>English Name</th>
                        <th>Arabic Name</th>
                        <th class="text-end" style="width: 200px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <!-- English Name -->
                            <td class="text-light font-weight-bold align-middle">@item.NameEn</td>
                            <!-- Arabic Name -->
                            <td class="text-light align-middle">@item.NameAr</td>
                            <!-- Actions -->
                            <td class="text-end align-middle">
                                <div class="d-flex justify-content-end gap-2">
                                    <a asp-area="Admin" asp-controller="Categories" asp-action="EditProductType" asp-route-id="@item.Id" class="btn btn-sm btn-premium btn-premium-secondary py-1 px-2" title="Edit">
                                        <i class="fa-solid fa-pen-to-square"></i>
                                    </a>
                                    
                                    <form asp-area="Admin" asp-controller="Categories" asp-action="DeleteProductType" asp-route-id="@item.Id" asp-route-subCategoryId="@subCategory.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this product type? Existing products referencing it will have their relationship restricted!');" style="display:inline;">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-premium btn-premium-danger py-1 px-2" title="Delete">
                                            <i class="fa-solid fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
